#include <stdio.h>

/**
 * main - prints the str with proper grammar,
 * but the outcome is a piece of art, the new line
 * Return: 0 when exited
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
