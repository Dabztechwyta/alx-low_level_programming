#include <stdio.h>

/**
 * main - prints the str "Programming is like building a multilingual puzzle"
 * Return : 0 if exited, none zer0 otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
